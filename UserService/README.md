### User Service

Hello hello, welcome to my thesis user service. This microservice is responsible for user management.

### How to run

1. Make sure you have .NET 10 installed
2. cd into UserService (if not already inside it)
3. Run `dotnet tool run dotnet-ef database update` to apply database migrations
4. Run using `dotnet run`